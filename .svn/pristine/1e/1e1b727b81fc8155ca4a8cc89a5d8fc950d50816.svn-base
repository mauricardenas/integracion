package com.grupogtd.integration.sicret.jsd.ws;

import com.grupogtd.alfresco.client.dto.AlfrescoRespuestaDTO;
import com.grupogtd.integration.sicret.jsd.ws.dto.DocumentoAdjuntoDTO;
import com.grupogtd.integration.sicret.jsd.ws.impl.AlfrescoUploaderImpl;
import org.apache.commons.io.IOUtils;
import org.junit.Test;

import java.io.File;
import java.io.FileInputStream;

import static com.grupogtd.integration.sicret.jsd.ws.constant.Constants.*;

public class AlfrescoUploaderIntegrationTest
{
    /*@Test
    public void uploadAlfresco()
    {
        try
        {
            DocumentoAdjuntoDTO documentoAdjuntoDTO = new DocumentoAdjuntoDTO();
            documentoAdjuntoDTO.setIdExterno(6135473L);
            documentoAdjuntoDTO.setNombreArchivo("prueba.txt");
            documentoAdjuntoDTO.setOrigen(ALFRESCO_TIPO_OBJ_RECLAMO);
            documentoAdjuntoDTO.setTipoObjeto("ARCHIVO_ADJUNTO");
            documentoAdjuntoDTO.setOrigen(ALFRESCO_ORIGEN_OBJETO);
            documentoAdjuntoDTO.setUsuario(ALFRESCO_USUARIO_SISTEMA);
            documentoAdjuntoDTO.setArchivo(IOUtils.toByteArray(new FileInputStream(new File("pom.xml"))));

            AlfrescoUploader uploader = new AlfrescoUploaderImpl();
            AlfrescoRespuestaDTO alfrescoRespuestaDTO = uploader.upload(documentoAdjuntoDTO);
            System.out.println(alfrescoRespuestaDTO);

        } catch (Exception e)
        {
            e.printStackTrace();
        }

*/


}